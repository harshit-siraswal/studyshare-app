<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the Android Window while the process is starting when the OS's Dark Mode setting is off -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@color/splash_background</item>
    </style>

    <!-- Shared style for notice-board widget items -->
    <style name="WidgetNoticeItem">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@drawable/widget_notice_item_background</item>
        <item name="android:ellipsize">end</item>
        <item name="android:maxLines">2</item>
        <item name="android:paddingStart">14dp</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingEnd">14dp</item>
        <item name="android:paddingBottom">12dp</item>
        <item name="android:textColor">@color/widget_notice_item_text</item>
        <item name="android:textSize">@dimen/widget_item_text_size</item>
    </style>

    <!-- Widget item text style with RTL support -->
    <style name="WidgetItemText">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@drawable/widget_item_background</item>
        <item name="android:ellipsize">end</item>
        <item name="android:maxLines">2</item>
        <item name="android:paddingStart">14dp</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingEnd">14dp</item>
        <item name="android:paddingBottom">12dp</item>
        <item name="android:textColor">@color/widget_item_text_color</item>
        <item name="android:textSize">@dimen/widget_item_text_size</item>
    </style>

    <style name="WidgetNoticeFeatureItem" parent="@style/WidgetNoticeItem">
        <item name="android:background">@drawable/widget_notice_paper_background</item>
        <item name="android:maxLines">2</item>
        <item name="android:textColor">@color/widget_notice_heading_text</item>
        <item name="android:textSize">15sp</item>
    </style>

    <style name="WidgetFeatureItem" parent="@style/WidgetItemText">
        <item name="android:maxLines">2</item>
        <item name="android:textSize">15sp</item>
    </style>
</resources>
